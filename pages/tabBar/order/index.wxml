<!--订单明细栏，记载的是之前的所有的订单 --ykg-->
<view>
  <import src="../../form/form" />
  <view wx:for="{{previewLRs}}" wx:key="rightText" wx:for-item="previewLR">
    <template is="previewLR" data="{{...previewLRs[index]}}" />
  </view>
</view>
